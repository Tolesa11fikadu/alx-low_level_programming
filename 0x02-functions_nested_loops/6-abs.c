#include "main.h"
#include <stdio.h>
/**
* _abs - function that computes the absolute value of integer
* @n: parameter
* Return: absolute value of a number
*/
int _abs(int)
{
if (n < 0)
n = -n
return (n);
}
